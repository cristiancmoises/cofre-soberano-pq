target/
/dist/
/.cofre-release-work.*/
*.qa

# Audit-signer key material — never commit. Covers v1.0.0 fixed names
# (qaudit.sk/qaudit.pk) and v1.0.1 derived names (<log-stem>.sk/.pk),
# plus the qgateway audit-keygen artifacts (.audit.skid / .audit.pub).
qaudit.sk
qaudit.pk
*.skid
*.audit.pub
# Generic .sk / .pk catch-all — the build never writes these into the
# source tree, so any that appear are operator-generated.
*.sk
*.pk

.idea/
.vscode/
*.swp
.DS_Store
